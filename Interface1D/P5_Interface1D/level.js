// Consturct a level that initialize the position of players and boxes

class Level {
    constructor(_levelNumber, _displaySize, _playerPosition, _boxPosition) {
        this.levelNumber = _levelNumber;
        this.displaySize = _displaySize;
        this.playerPosition = _playerPosition;
        this.boxPosition = _boxPosition;
    }

}